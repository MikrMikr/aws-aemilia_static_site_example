version: 1
applications:
  - frontend:
      phases:
        build:
          commands:
          #- echo "AWS_AMPLIFY_DEPLOYMENT_ID=banana" >> backend/.env 
          - echo 'export AWS_AMPLIFY_DEPLOYMENT_ID="banana"' >> ~/.bashrs 
          - export AWS_AMPLIFY_DEPLOYMENT_ID="banana"
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: .
