version: 1
applications:
  - frontend:
      phases:
        build:
          commands:
          #- echo "AWS_AMPLIFY_DEPLOYMENT_ID=banana" >> backend/.env 
          - echo 'export AWS_AMPLIFY_DEPLOYMENT_ID="banana8"' >> ~/.bashrs 
          - export AWS_AMPLIFY_DEPLOYMENT_ID="banana8"
      artifacts:
        baseDirectory: /
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: .
